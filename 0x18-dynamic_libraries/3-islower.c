#include "main.h"

/**
 * _islower - review for all the lowercase characters
 * @c: character that will be checked
 * Return: 1 if c is lowercase, return 0 if not lowercase
 */
int _islower(int c)

{
	return (c >= 'a' && c <= 'z');
}
