#include "main.h"

/**
 * _isdigit - will be checking for a digit 0 until 9)
 * @c: int that will be checked
 * Return: 1 if c is a digit, 0 if it is not a digit
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
